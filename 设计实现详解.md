# lab7 cal 命令 — 设计实现详解

> 本文档面向 C++ 初学者，详细讲解项目的架构设计、每个模块的实现细节、
> 以及用到的 C++ 新特性。建议配合源码一起阅读。

---

## 目录

1. [总体架构](#1-总体架构)
2. [FlagDescriptor — 把命令行参数抽象为对象](#2-flagdescriptor)
3. [FlagTable — 纯容器](#3-flagtable)
4. [OptionParser — 通用解析循环](#4-optionparser)
5. [CalOptions — 选项数据与月份展开](#5-caloptions)
6. [MonthCalendar — 单月月历生成](#6-monthcalendar)
7. [Calendar — 多月编排与横向拼接](#7-calendar)
8. [main.cpp — 粘合代码](#8-maincpp)
9. [错误处理机制](#9-错误处理机制)
10. [C++ 新特性速查](#10-c-新特性速查)

---

## 1. 总体架构

### 1.1 项目文件结构

```
lab7/
├── CMakeLists.txt               ← 构建配置（C++23，零外部依赖）
├── readme.md                    ← 设计文档（类图、关系图、用例图）
├── 设计实现详解.md               ← 本文档
├── src/                         ← 源代码
│   ├── main.cpp                 ← 入口
│   ├── cal_options.hpp/cpp      ← 选项数据结构
│   ├── flag_descriptor.hpp/cpp  ← flag 元数据
│   ├── flag_table.hpp/cpp       ← flag 容器
│   ├── option_parser.hpp/cpp    ← 命令行解析
│   ├── month_calendar.hpp/cpp   ← 单月月历
│   └── calendar.hpp/cpp         ← 多月编排
└── test/                        ← 测试代码
    ├── test_harness.hpp         ← 自制测试框架
    ├── test_main.cpp
    ├── test_month_calendar.cpp  ← 单月单元测试
    ├── test_option_parser.cpp   ← 解析器单元测试
    ├── test_cal_options.cpp     ← resolve() 单元测试
    └── test_integration.cpp     ← 集成测试
```

### 1.2 数据流

一条命令从输入到输出的完整路径：

```
用户敲: cal -A 2 -m 5 2025
        │
        ▼
argv[]: ["cal", "-A", "2", "-m", "5", "2025"]
        │
        ▼
┌───────────────────────────────────────────────┐
│ OptionParser::parse(argv, FlagTable)           │
│                                                │
│ 逐 token 遍历 argv:                             │
│   "-A" → table.find("-A") → apply(opts, "2")  │
│   "-m" → table.find("-m") → apply(opts, "5")  │
│   "2025" → 位置参数 → apply(opts, "2025")     │
│                                                │
│ 输出: CalOptions{ after=2, month=5, year=2025 }│
└───────────────────────┬───────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────┐
│ CalOptions::resolve(today)                     │
│                                                │
│ y = 2025, m = 5                                │
│ 不是"只指定年份" → 以 (2025,5) 为中心展开       │
│ start = 往前退 0 个月 = (2025, 5)              │
│ count = 1 + 0 + 2 = 3                          │
│                                                │
│ 输出: [(2025,5), (2025,6), (2025,7)]           │
└───────────────────────┬───────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────┐
│ Calendar::generate(CalOptions, today)          │
│                                                │
│ resolve → [(2025,5), (2025,6), (2025,7)]      │
│ group_by_rows(3个月, 每排3个) → [[5,6,7]]     │
│ render_row([May, June, July]) → 横向拼接       │
│                                                │
│ 输出: 一个完整的月历字符串                      │
└───────────────────────┬───────────────────────┘
                        │
                        ▼
                   stdout（终端）
```

### 1.3 类关系一览

```
main()
  │
  │ ① 调用 build_flag_table() 注册所有 flag
  ▼
FlagDescriptor × N ───聚合───► FlagTable
                                   │
  │ ② 传 argv + table 进去         │
  ▼                              │
OptionParser ─────依赖─────────────┘
  │
  │ ③ 返回 CalOptions
  ▼
CalOptions ──► resolve(today) ──► list<(年,月)>
                                      │
  │                                   │ ④ 每个 (年,月) 构造一个
  │                                   ▼
  │                            MonthCalendar × N
  │                                   │
  │ ⑤ 传给 Calendar                   │
  ▼                                   │
Calendar ────聚合─────────────────────┘
  │
  │ ⑥ generate() 返回最终的月历字符串
  ▼
std::print()
```

---

## 2. FlagDescriptor

### 2.1 问题：为什么需要这个类

传统的命令行解析写法：

```cpp
// ❌ 坏设计：每加一个 flag 就要加一个 if-else 分支
while (i < argc) {
    if (argv[i] == "-A") {
        opts.after_months = atoi(argv[++i]);
    } else if (argv[i] == "-B") {
        opts.before_months = atoi(argv[++i]);
    } else if (argv[i] == "-m") {
        opts.month = atoi(argv[++i]);
    } else if (argv[i] == "-d") {
        // 20 行解析 yyyy-mm 的代码 ...
    }
    // ... 每加一个新参数，这个函数就会变得更长
}
```

问题：

| 问题 | 说明 |
|------|------|
| 解析函数无限增长 | 6 个参数就几十行，20 个参数不敢想 |
| 违反开闭原则 | 加新参数必须改解析循环 |
| 职责混杂 | 解析逻辑和参数语义混在一起 |

### 2.2 核心思想

观察：**每个命令行参数只有三个东西不一样**——

1. **名字**是什么？（`"-A"`、`"-m"`、`""`表示位置参数）
2. **需要什么类型的参数**？（不需要 / 整数 / yyyy-mm 字符串）
3. **怎么把值写入 CalOptions**？（存到哪个字段、如何转换）

那就封装成一个对象：

```cpp
// flag_descriptor.hpp

enum class ArgKind {
    None,      // 无参数
    Int,       // 整数参数
    DateStr    // yyyy-mm 格式字符串参数
};

class FlagDescriptor {
public:
    std::string flag;       // flag 名称，"" 代表位置参数（即裸 yyyy）
    ArgKind    arg_kind;    // 需要的参数类型
    std::function<void(CalOptions&, const std::string&)> apply; // 写入回调
};
```

`apply` 的类型是 `std::function<void(CalOptions&, const std::string&)>`，意思是"可以装下任何接收 `(CalOptions&, const std::string&)` 参数、返回 `void` 的可调用物"。

### 2.3 实例化：每个 flag 一个对象

在 `main.cpp` 的 `build_flag_table()` 里创建：

```cpp
// -A 2 → 把 "2" 转成 int，存到 CalOptions::after_months
FlagDescriptor("-A", ArgKind::Int,
    [](CalOptions& opts, const std::string& val) {
        opts.after_months = std::stoi(val);
    }
);

// -d 2025-03 → 解析 yyyy-mm，存到 CalOptions::date
FlagDescriptor("-d", ArgKind::DateStr,
    [](CalOptions& opts, const std::string& val) {
        auto pos = val.find('-');
        int y = std::stoi(val.substr(0, pos));
        int m = std::stoi(val.substr(pos + 1));
        opts.date = {y, m};
    }
);

// 位置参数 2025 → 把 "2025" 转成 int，存到 CalOptions::year
FlagDescriptor("", ArgKind::Int,        // ← 空串名 = 位置参数
    [](CalOptions& opts, const std::string& val) {
        opts.year = std::stoi(val);
    }
);
```

### 2.4 int_flag 工厂函数

`-A`、`-B`、`-m`、`-r` 都是整数类型、都是存到 `CalOptions` 的某个 `int` 字段。为了避免四遍重复代码，写一个工厂函数：

```cpp
auto int_flag = [](std::string name, auto member_ptr) {
    return FlagDescriptor(
        name, ArgKind::Int,
        [member_ptr](CalOptions& opts, const std::string& val) {
            opts.*member_ptr = std::stoi(val);
        }
    );
};
```

`auto member_ptr` 是 C++ 的**成员指针**。类似普通指针指向一个变量，成员指针指向类/结构体的一个成员字段：

```cpp
// 普通指针
int x = 5;
int* p = &x;
*p = 10;       // 通过指针修改 x

// 成员指针
int CalOptions::*mp = &CalOptions::month;
CalOptions opts;
opts.*mp = 5;  // 通过成员指针修改 opts.month
```

使用时一行搞定：

```cpp
table.register_flag(int_flag("-A", &CalOptions::after_months));
table.register_flag(int_flag("-B", &CalOptions::before_months));
table.register_flag(int_flag("-m", &CalOptions::month));
table.register_flag(int_flag("-r", &CalOptions::rows_per_line));
```

---

## 3. FlagTable

### 3.1 职责

FlagTable 的职责单一到不能再单一：**把 FlagDescriptor 存起来，按名称查找出去**。

```cpp
// flag_table.hpp

class FlagTable {
public:
    void register_flag(const FlagDescriptor& desc);            // 存
    [[nodiscard]] const FlagDescriptor* find(const std::string&) const; // 查
private:
    std::vector<FlagDescriptor> flags_;
};
```

它不包含任何解析逻辑。这是 SRP（单一职责原则）的体现：

> 一个类应该只有一个变化的理由。FlagTable 变化的理由只有"flag 的存储/查找方式变了"。

### 3.2 实现

```cpp
// flag_table.cpp

void FlagTable::register_flag(const FlagDescriptor& desc) {
    flags_.push_back(desc);
}

const FlagDescriptor* FlagTable::find(const std::string& token) const {
    for (const auto& desc : flags_) {
        if (desc.flag == token) {
            return &desc;       // 找到，返回指针
        }
    }
    return nullptr;             // 没找到
}
```

线性查找对 5~10 个 flag 来说完全够用。`find("")` 返回位置参数的描述符。

---

## 4. OptionParser

### 4.1 核心：通用解析循环

OptionParser 是整个设计最体现**开闭原则**（Open-Closed Principle）的地方。

> **开闭原则**：对扩展开放（加新功能容易），对修改关闭（不改已有代码）。

解析循环只做**查表 → 委托**，不需要知道具体有哪些 flag：

```cpp
// option_parser.cpp（简化版）

CalOptions OptionParser::parse(int argc, char* argv[],
                                const FlagTable& table) const {
    CalOptions opts;
    int i = 1;  // 跳过 argv[0]（程序名）

    while (i < argc) {
        std::string token(argv[i]);

        // ─── 情况 1：token 匹配已注册 flag ───
        const FlagDescriptor* desc = table.find(token);
        if (desc != nullptr) {
            std::string arg;
            if (desc->arg_kind != ArgKind::None) {
                ++i;
                if (i >= argc) {
                    throw std::runtime_error("'" + token + "' 后面缺少参数");
                }
                arg = argv[i];
            }
            // 🔑 关键：委托 apply()，不关心具体写哪个字段
            desc->apply(opts, arg);
        }

        // ─── 情况 2：裸数字 → 当位置参数（年份）处理 ───
        else if (std::isdigit(token[0])) {
            table.find("")->apply(opts, token);
        }

        // ─── 情况 3：都不匹配 → 报错 ───
        else {
            throw std::runtime_error("未知选项: " + token);
        }

        ++i;
    }
    return opts;
}
```

### 4.2 一次具体执行的跟踪

以 `cal -A 2 -m 5 2025` 为例，一步一步走过解析循环：

```
argv = ["cal", "-A", "2", "-m", "5", "2025"]

┌─────────────────────────────────────────────────────────────────┐
│ i=1  token="-A"                                                  │
│                                                                  │
│ table.find("-A") → 找到了，arg_kind=Int                         │
│ 需要取参数 → i++ → arg="2"                                      │
│ desc->apply(opts, "2") → opts.after_months = 2                  │
│                                                                  │
│ CalOptions: { after_months: 2 }                                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ i=3  token="-m"                                                  │
│                                                                  │
│ table.find("-m") → 找到了，arg_kind=Int                         │
│ 需要取参数 → i++ → arg="5"                                      │
│ desc->apply(opts, "5") → opts.month = 5                         │
│                                                                  │
│ CalOptions: { after_months: 2, month: 5 }                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ i=5  token="2025"                                                │
│                                                                  │
│ table.find("2025") → nullptr（没有名字叫 "2025" 的 flag）       │
│ isdigit('2') → true → 当位置参数                                │
│ table.find("") → 找到了位置参数的描述符                         │
│ desc->apply(opts, "2025") → opts.year = 2025                    │
│                                                                  │
│ CalOptions: { after_months: 2, month: 5, year: 2025 }           │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 对比坏设计

| | 坏设计（if-else 分支） | 好设计（FlagDescriptor 模式） |
|---|---|---|
| 加新参数 | 改 `parse()` 函数，加分支 | 在 `build_flag_table()` 加一行 `register_flag()` |
| `parse()` 长度 | 随参数数量线性增长 | 永远 ~30 行，不随参数增长 |
| 参数逻辑位置 | 散落在 parse 的大函数里 | 封装在各自 FlagDescriptor 的 apply 中 |
| 开闭原则 | 违反 | 遵循 |

---

## 5. CalOptions

### 5.1 数据结构

```cpp
// cal_options.hpp

struct CalOptions {
    int after_months  = 0;                      // -A
    int before_months = 0;                      // -B
    std::optional<int> month;                   // -m
    std::optional<int> year;                    // 位置参数 yyyy
    std::optional<std::pair<int, int>> date;    // -d yyyy-mm
    int rows_per_line = 3;                      // -r
};
```

`std::optional` 的含义：`month` 可能有值（用户指定了 `-m 5`），也可能没有（用户没指定）。用 `std::optional` 比用 -1 或 0 表示"未设置"语义更清晰、更安全。

### 5.2 `resolve()` — 核心算法

`resolve()` 是 CalOptions 唯一的非平凡方法。它把原始选项展开为一个**有序的 (年, 月) 列表**。

```cpp
std::vector<std::pair<int, int>> CalOptions::resolve(
    std::pair<int, int> today) const {

    // ── 第一步：确定年份 ──
    // 优先级：year（位置参数） > date.year（来自 -d） > today.year
    int y = year.value_or(
        date.has_value() ? date->first : today.first);

    // ── 第二步：确定月份 ──
    // 优先级：month（来自 -m） > date.month（来自 -d） > today.month
    int m = month.value_or(
        date.has_value() ? date->second : today.second);

    // ── 第三步：判断"用户是否只指定了年份" ──
    // 条件：显式给了年份，且没有通过 -m 或 -d 指定月份
    bool only_year_specified = year.has_value()
                            && !month.has_value()
                            && !date.has_value();

    std::pair<int, int> start;
    int count;

    if (only_year_specified) {
        // cal 2025 → 显示全年 12 个月
        start = {y, 1};
        count = 12;
    } else {
        // cal -m 5 -A 2 → 以 (y,m) 为中心，前后扩展
        start = add_months({y, m}, -before_months);
        count = 1 + before_months + after_months;
    }

    // ── 第四步：构建列表 ──
    std::vector<std::pair<int, int>> result;
    for (int i = 0; i < count; ++i) {
        result.push_back(add_months(start, i));
    }
    return result;
}
```

### 5.3 `add_months()` — 跨年月偏移

这是一个文件内部的辅助函数（匿名命名空间，对外不可见）：

```cpp
std::pair<int, int> add_months(std::pair<int, int> ym, int delta) {
    int y = ym.first;
    int m = ym.second + delta;    // 可能 < 1 或 > 12

    if (m > 0) {
        y += (m - 1) / 12;
        m = (m - 1) % 12 + 1;
    } else {
        // m <= 0 时，C++ 整数除法向零取整，需要手动调成向下取整
        int years_back = (-m) / 12 + 1;
        y -= years_back;
        m += years_back * 12;
    }
    return {y, m};
}
```

例如 `add_months({2025, 1}, -2)` → `{2024, 11}`，`add_months({2025, 12}, 2)` → `{2026, 2}`。

### 5.4 `only_year_specified` 为什么必须检查 `year.has_value()`

最初的设计没有这个检查：

```cpp
// ❌ 错误版本
bool only_year_specified = !month.has_value() && !date.has_value();
```

这会导致裸 `cal`（无任何参数）也被当成"显示全年"——因为此时 `month` 和 `date` 确实都没有值。正确的判断是：

```cpp
// ✅ 正确版本
bool only_year_specified = year.has_value()
                        && !month.has_value()
                        && !date.has_value();
```

**"只指定年份"**的前提是**你确实指定了年份**。裸 `cal` 应该显示当前月份，而不是全年。

---

## 6. MonthCalendar

### 6.1 职责和对外接口

MonthCalendar 生成**单个月份的等宽文本块**。每一行恰好 21 个字符宽。对外只暴露三个方法：

```cpp
class MonthCalendar {
public:
    static constexpr int kWidth = 21;  // 每行固定宽度

    MonthCalendar(int year, int month);

    [[nodiscard]] std::string title_line() const;    // "      May 2025"
    [[nodiscard]] std::string header_line() const;   // "Su Mo Tu We Th Fr Sa"
    [[nodiscard]] std::vector<std::string> week_lines() const; // 4-6 行日期
};
```

### 6.2 文本块结构

一个典型月份的完整输出：

```
      May 2025           ← title_line()  [第 0 行]
Su Mo Tu We Th Fr Sa     ← header_line() [第 1 行]
          1  2  3         ← week_lines()[0] [第 2 行]
 4  5  6  7  8  9 10      ← week_lines()[1] [第 3 行]
11 12 13 14 15 16 17      ← week_lines()[2] [第 4 行]
18 19 20 21 22 23 24      ← week_lines()[3] [第 5 行]
25 26 27 28 29 30 31      ← week_lines()[4] [第 6 行]
```

每行 21 列：7 个格子 × 3 列（日期数字 2 位 + 1 空格）。所有行的宽度一致，这是多月并排的前提。

### 6.3 模块内部实现

#### 6.3.1 月份天数

```cpp
int MonthCalendar::days_in_month() const {
    static constexpr std::array<int, 12> days = {
        31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};
    int d = days[month_ - 1];
    if (month_ == 2) {
        // 闰年规则：能被 4 整除但不能被 100 整除，或者能被 400 整除
        bool leap = (year_ % 4 == 0 && year_ % 100 != 0)
                 || (year_ % 400 == 0);
        if (leap) d = 29;
    }
    return d;
}
```

`static constexpr` 表示这个数组在**编译时**就固化了，运行时零开销，就像一个写死在代码段里的常量表。

#### 6.3.2 每月 1 号是星期几 — Sakamoto 算法

```cpp
int MonthCalendar::first_weekday() const {
    int y = year_;
    int m = month_;    // ⚠️ 保持原始的 1-based 月份

    // 一月和二月：年份减 1
    if (m < 3) {
        y -= 1;
    }

    // Sakamoto 魔数表，按原始月份（1–12）索引
    static constexpr std::array<int, 12> t = {
        0, 3, 2, 5, 0, 3, 5, 1, 4, 6, 2, 4};

    return (y + y/4 - y/100 + y/400 + t[m - 1] + 1) % 7;
    //     └──────────────────┬──────────────────┘  └─┬─┘
    //              年份的累计偏移                   日期=1号
    // 返回值: 0=周日, 1=周一, ..., 6=周六
}
```

**踩坑提醒**：Sakamoto 算法中，一月和二月的年份减少了 1，但**月份本身不修改**。如果你把 `m` 也加 12，那 `t[m-1]` 会越界访问（`t[13]`）→ 未定义行为。

#### 6.3.3 标题行

```cpp
std::string MonthCalendar::title_line() const {
    std::string title = std::format("{} {}", month_name(), year_);
    // 例如 title = "May 2025"，长度 8

    // 在 kWidth(21) 内居中
    int pad_total = 21 - 8 = 13;
    int pad_left  = 13 / 2 = 6;
    int pad_right = 13 - 6 = 7;
    // 结果: "      May 2025       "
}
```

#### 6.3.4 日期行的生成

```cpp
std::vector<std::string> MonthCalendar::week_lines() const {
    int first_wd  = first_weekday();   // 1 号是星期几
    int total     = days_in_month();   // 本月多少天
    int rows      = (first_wd + total + 6) / 7;  // 向上取整 → 4~6 行

    int day = 1;
    for (int r = 0; r < rows; ++r) {
        std::string line;
        for (int col = 0; col < 7; ++col) {
            if (col > 0) line += ' ';  // 列间空格

            if ((r == 0 && col < first_wd) || day > total) {
                line += "  ";          // 空白格
            } else {
                line += std::format("{:>2}", day);  // 右对齐两位
                ++day;
            }
        }
        line.append(21 - line.size(), ' ');  // 右侧补齐
        result.push_back(line);
    }
}
```

以 2025 年 5 月为例（1 号是周四，first_wd=4，31 天）：

```
first_wd=4, total=31, cells=35, rows=(35+6)/7=5

第 0 行 (r=0):
  col=0,1,2,3: < first_wd → "  " (4 个空格)
  col=4: day=1 → " 1"
  col=5: day=2 → " 2"
  col=6: day=3 → " 3"
  → "          1  2  3"

第 1 行 (r=1):
  col=0~6: day=4~10
  → " 4  5  6  7  8  9 10"

...

第 4 行 (r=4):
  col=0~6: day=25~31
  → "25 26 27 28 29 30 31"
```

---

## 7. Calendar

### 7.1 职责

Calendar 负责两个工作：

1. **把扁平月份列表按横排分组**（`group_by_rows`）
2. **把一排的所有 MonthCalendar 横向拼接**（`render_row`）

### 7.2 按排分组

```cpp
std::vector<std::vector<MonthCalendar>> Calendar::group_by_rows(
    const std::vector<std::pair<int, int>>& months, int per_row) {

    std::vector<std::vector<MonthCalendar>> rows;
    std::vector<MonthCalendar> current_row;

    for (const auto& [y, m] : months) {
        current_row.emplace_back(y, m);         // 加到当前排

        if (current_row.size() == per_row) {    // 排满了
            rows.push_back(std::move(current_row));  // 保存这排
            current_row.clear();                // 开始新一排
        }
    }

    if (!current_row.empty()) {
        rows.push_back(std::move(current_row)); // 最后不满的一排
    }
    return rows;
}
```

例如 `cal 2025` 产生 12 个月，`per_row=3`：

```
输入: [1月, 2月, 3月, 4月, 5月, 6月, 7月, 8月, 9月, 10月, 11月, 12月]
输出:
  排 0: [1月, 2月, 3月]
  排 1: [4月, 5月, 6月]
  排 2: [7月, 8月, 9月]
  排 3: [10月, 11月, 12月]
```

### 7.3 横向拼接 — 最关键的部分

这是整个项目中最复杂的算法。核心思想：**逐行拼接**。

```
一月                        二月                        三月
                                                       (第7行是空白补齐)
───────────                ───────────                ───────────
[0]    January 2025  ←→   [0]   February 2025  ←→   [0]     March 2025
[1]  Su Mo Tu ...    ←→   [1]  Su Mo Tu ...    ←→   [1]   Su Mo Tu ...
[2]  ...1  2  3...   ←→   [2]  ...1...         ←→   [2]   ...1...
[3]  ...4  5  6...   ←→   [3]  ...2  3  4...   ←→   [3]   ...2  3  4...
[4]  ...             ←→   [4]  ...             ←→   [4]   ...
[5]  ...             ←→   [5]  ...             ←→   [5]   ...
[6]  21个空格(补齐)   ←→   [6]  ...             ←→   [6]   ...
                                                 ←→   [7]   ...
       ↑ 3空格间距           ↑ 3空格间距
```

**每列宽度不一致时的处理**：不同月份的行数可能不一样（4、5 或 6 行日期）。先遍历一行中所有月份，找到最大行数，然后行数不够的月份用 21 个空格补全：

```cpp
std::string Calendar::render_row(const std::vector<MonthCalendar>& row) {
    // ── 第一步：收集每个月的文本块，找最大行数 ──
    int max_lines = 0;
    std::vector<Block> blocks;

    for (const auto& mc : row) {
        Block b;
        b.title  = mc.title_line();     // 第 0 行
        b.header = mc.header_line();    // 第 1 行
        b.weeks  = mc.week_lines();     // 第 2~N 行
        // b 的总行数 = 2 + week_lines().size()
        max_lines = std::max(max_lines, b.total_lines());
        blocks.push_back(std::move(b));
    }

    std::string spacing(3, ' ');  // 月份间距 3 空格

    // ── 第二步：逐行拼接 ──
    std::string output;
    for (int line = 0; line < max_lines; ++line) {
        for (int bi = 0; bi < blocks.size(); ++bi) {
            const auto& b = blocks[bi];

            if (bi > 0) output += spacing;  // 月份之间加 3 空格

            if (line == 0) {
                output += b.title;           // 标题行
            } else if (line == 1) {
                output += b.header;          // 表头行
            } else {
                int week_idx = line - 2;
                if (week_idx < b.weeks.size()) {
                    output += b.weeks[week_idx]; // 该月有这一行
                } else {
                    output += std::string(21, ' '); // 该月没这行，补齐空格
                }
            }
        }
        // 去掉行尾空格，换行
        while (!output.empty() && output.back() == ' ') {
            output.pop_back();
        }
        output += '\n';
    }
    return output;
}
```

### 7.4 顶层入口

```cpp
std::string Calendar::generate(const CalOptions& opts,
                               std::pair<int, int> today) const {
    auto months = opts.resolve(today);               // 展开月份列表
    auto rows   = group_by_rows(months, opts.rows_per_line);  // 分组

    std::string result;
    for (int i = 0; i < rows.size(); ++i) {
        if (i > 0) result += '\n';  // 排与排之间空一行
        result += render_row(rows[i]);
    }
    return result;
}
```

---

## 8. main.cpp

### 8.1 整体结构

```cpp
int main(int argc, char* argv[]) {
    try {
        FlagTable table = build_flag_table();      // ① 注册所有 flag
        OptionParser parser;
        CalOptions opts = parser.parse(argc, argv, table); // ② 解析
        auto today = today_ym();                   // ③ 获取今天日期
        Calendar cal;
        std::string output = cal.generate(opts, today);    // ④ 生成
        std::print("{}", output);                  // ⑤ 输出
        return EXIT_SUCCESS;
    } catch (const std::exception& e) {
        std::println(stderr, "cal: {}", e.what()); // 统一错误处理
        return EXIT_FAILURE;
    }
}
```

**5 行完成所有工作**，因为大量逻辑委托给了各个类。

### 8.2 `today_ym()` — 获取系统日期

```cpp
std::pair<int, int> today_ym() {
    auto now = std::chrono::system_clock::now();          // 获取当前时间点
    auto days = std::chrono::floor<std::chrono::days>(now); // 截断到天精度
    auto ymd = std::chrono::year_month_day{days};         // 分离年/月/日
    return {static_cast<int>(ymd.year()),
            static_cast<unsigned>(ymd.month())};
}
```

使用 C++20 的 `std::chrono` 日历扩展，不需要系统调用或第三方库。

### 8.3 `build_flag_table()` — 唯一知道有哪些 flag 的地方

这是整个项目中**唯一**和具体命令行参数耦合的代码。如果将来要加新参数（比如 `-3` 显示三个月），只需要在这里加一行 `table.register_flag(...)`，其余文件完全不用动。

---

## 9. 错误处理机制

项目采用**异常 + 统一捕获**的错误处理策略：

### 9.1 错误来源

| 错误类型 | 抛出位置 | 示例 |
|---------|---------|------|
| 未知 flag | OptionParser | `cal --bogus` → `未知选项: --bogus` |
| 参数缺失 | OptionParser | `cal -m` → `'-m' 后面缺少参数` |
| 值格式错误 | FlagDescriptor.apply | `cal -d abc` → `-d 的值无效: 需要 yyyy-mm 格式` |
| 值范围错误 | FlagDescriptor.apply | `cal -m 13` → `-m 的值无效: 月份必须在 [1, 12]` |
| 年份范围错误 | FlagDescriptor.apply | `cal 0` → `年份 '0' 无效: 年份必须在 [1, 9999]` |

### 9.2 统一处理

所有异常都在 `main()` 的 `catch` 块中统一处理：

```cpp
catch (const std::exception& e) {
    std::println(stderr, "cal: {}", e.what());
    return EXIT_FAILURE;
}
```

错误信息输出到 **stderr**（不是 stdout），进程以非零状态退出。

### 9.3 MonthCalendar 构造时的校验

除了命令行解析，MonthCalendar 构造函数也会做范围校验（防御性编程）：

```cpp
MonthCalendar::MonthCalendar(int year, int month) {
    if (month < 1 || month > 12)
        throw std::invalid_argument("月份超出范围");
    if (year < 1 || year > 9999)
        throw std::invalid_argument("年份超出范围");
}
```

---

## 10. C++ 新特性速查

项目中用到的 C++ 新特性及它们的作用：

| 特性 | C++ 版本 | 在项目中用于 |
|------|---------|-------------|
| `std::optional<T>` | C++17 | `CalOptions::month/year/date` 表达"可选"语义 |
| `std::function` | C++11 | `FlagDescriptor::apply` 存储任意可调用物 |
| Lambda 表达式 | C++11 | 定义 `apply` 回调、`int_flag` 工厂 |
| 成员指针 `T::*` | C++98 | `int_flag` 用一个工厂生成四个不同的 setter |
| `auto` 类型推导 | C++11 | 简化复杂类型名 |
| `[[nodiscard]]` | C++17 | 标记返回值不应被丢弃的函数 |
| `std::pair` + 结构化绑定 | C++17 | `for (auto& [y, m] : months)` |
| `std::format` / `std::print` | C++23 | 类型安全的格式化输出 |
| `std::chrono` 日历 | C++20 | 获取系统日期（年/月/日） |
| `static constexpr` | C++17 | 编译期常量表（天数、魔数、名称） |
| `std::array` | C++11 | 比 C 数组更安全的固定大小数组 |
| `std::source_location` | C++20 | 测试框架中报告失败位置 |
| `enum class` | C++11 | `ArgKind` 强类型枚举，避免命名冲突 |

---

## 附录：SOLID 原则在本项目中的体现

| 原则 | 项目中的体现 |
|------|-------------|
| **S** — 单一职责 | 6 个类，每个只有一个职责。`OptionParser` 只管"查表→委托"的循环逻辑；`FlagTable` 只管存储和查找；`MonthCalendar` 只管单月渲染 |
| **O** — 开闭原则 | 加新命令行参数 = 在 `build_flag_table()` 加一行 `register_flag()`，不修改 `OptionParser`；加新输出格式 = 扩展 `Calendar`，不修改 `MonthCalendar` |
| **L** — 里氏替换 | 暂无继承层次。若未来需要继承，`FlagDescriptor` 的 `apply` 回调策略模式为子类化提供了替代方案 |
| **I** — 接口隔离 | `FlagTable` 只暴露 `find()` + `register_flag()`；`MonthCalendar` 只暴露 3 个渲染方法 + 2 个 getter；最小的调用者负担 |
| **D** — 依赖倒置 | `OptionParser` 依赖 `FlagTable`（稳定的抽象容器），不依赖具体的 flag 实现；`Calendar` 依赖 `MonthCalendar` 的公共接口，不依赖其内部实现 |
